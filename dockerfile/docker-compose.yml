version: "3"
services:
  app:
    build:
      context: ..
      dockerfile: dockerfile/Dockerfile
    # app name
    image: shiitake
    # portmapping, 3000 port but 1337 is local
    ports: 
      - 13000:3000
    env_file:
      - .env 